version: '3.1'

services:
    app:
        image: 'my-angular-app'
        build:
          context: '.'
          dockerfile: Dockerfile
        environment:
          - VIRTUAL_HOST=egresadosunicauca.tk,www.egresadosunicauca.tk
          - LETSENCRYPT_HOST=egresadosunicauca.tk,www.egresadosunicauca.tk
          - LETSENCRYPT_EMAIL=egresado0809@gmail.com
networks:
  default:
    external:
      name: work
