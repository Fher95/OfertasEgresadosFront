import { FiltrarCiudadesPipe } from './filtrar-ciudades.pipe';

describe('FiltrarCiudadesPipe', () => {
  it('create an instance', () => {
    const pipe = new FiltrarCiudadesPipe();
    expect(pipe).toBeTruthy();
  });
});
