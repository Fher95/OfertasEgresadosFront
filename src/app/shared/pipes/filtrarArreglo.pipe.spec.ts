import { FiltrarArregloPipe } from './filtrarArreglo.pipe';

describe('FiltrarArregloPipe', () => {
  it('create an instance', () => {
    const pipe = new FiltrarArregloPipe();
    expect(pipe).toBeTruthy();
  });
});
